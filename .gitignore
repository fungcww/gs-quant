*.pyc
build/
dist/
*.egg-info/
gs_quant.egg-info/

gs_quant/.idea/
research_results/*

# Local research data (SQLite and other large, regenerable datasets)
shared_data/
charts/
*.db

# Local environment config — never commit secrets
.env

# Experiment outputs — ephemeral, never commit
research/experiments/outputs/scratch/
research/experiments/outputs/artifacts/
